//prettier-ignore
// fixture from modify-json-file and originally from fork of some lib for tsconfig
{
    // Compiler options.
    "compilerOptions": {
        "module": "commonjs",
        "noImplicitAny": true,
        "removeComments": true,
        "preserveConstEnums": true,
        "outDir": "dist",
        "sourceMap": true, // regular dangling comma
    } ,
    // These are the files to compile with.
    "files": [
        "./src/foo.ts" /* Use `foo.ts` */ , // dangling comma surrounded with whitespace and comments
    ]
  // dangling comma on next line
  ,
}
